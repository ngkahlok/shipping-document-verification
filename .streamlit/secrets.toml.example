# Copy this file to .streamlit/secrets.toml (already gitignored) and fill in
# your real key. Streamlit reads this file automatically -- app.py's secrets
# bridge then copies it into os.environ so the rest of the pipeline code
# (which reads plain env vars) works unchanged.
GEMINI_API_KEY = "AQ.Ab8RN6J4_V39Pes8qGjNtxKvw04ACTEdgHwmEIWvAy7Ubzq-fQ"
